# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Supabase connection details (the anon key is safe for the browser bundle,
# but keep it out of git anyway — copy .env.example to .env instead)
.env
.env.local

# Bundling artifacts (from the Parcel + html-inline single-file build)
bundle.html
.parcel-cache
favicon.svg

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

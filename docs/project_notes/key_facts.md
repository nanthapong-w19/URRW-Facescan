# Key Facts

Project configuration, constants, and non-sensitive reference info for face-checkin-app.

## ⚠️ SECURITY WARNING: What NOT to Store Here

This file is committed to version control. **Never** store passwords, API keys, tokens, service account keys, DB passwords, OAuth secrets, or any value from `.env`.

Secrets go in: `.env` (gitignored), a password manager, or a secrets manager — never here.

Safe to store: hostnames/ports, project IDs, public endpoint URLs, service account *emails* (not keys), env names, deployment targets.

## Format

Organize by category, bullet lists.

## Entries

_No entries yet._
